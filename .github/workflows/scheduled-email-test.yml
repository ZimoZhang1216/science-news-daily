name: Scheduled Email Test

on:
  schedule:
    # Temporary five-minute email test. The marker below prevents repeat sends.
    # Remove this workflow after confirming the scheduled email run.
    - cron: "*/5 * * * *"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: scheduled-email-test-${{ github.ref }}
  cancel-in-progress: false

jobs:
  send:
    name: Generate and email reports
    if: github.event_name == 'workflow_dispatch' || github.event.schedule == '*/5 * * * *'
    runs-on: ubuntu-latest
    timeout-minutes: 45
    env:
      EMAIL_ENABLED: "true"
      LLM_PROVIDER: ${{ secrets.LLM_PROVIDER }}
      OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
      OPENAI_MODEL: ${{ secrets.OPENAI_MODEL }}
      DEEPSEEK_API_KEY: ${{ secrets.DEEPSEEK_API_KEY }}
      DEEPSEEK_MODEL: ${{ secrets.DEEPSEEK_MODEL }}
      REPORT_EMAIL_TO: ${{ secrets.REPORT_EMAIL_TO }}
      CHEM_REPORT_EMAIL_TO: ${{ secrets.CHEM_REPORT_EMAIL_TO }}
      BIO_REPORT_EMAIL_TO: ${{ secrets.BIO_REPORT_EMAIL_TO }}
      STAT_REPORT_EMAIL_TO: ${{ secrets.STAT_REPORT_EMAIL_TO }}
      SMTP_HOST: ${{ secrets.SMTP_HOST }}
      SMTP_PORT: ${{ secrets.SMTP_PORT }}
      SMTP_USERNAME: ${{ secrets.SMTP_USERNAME }}
      SMTP_PASSWORD: ${{ secrets.SMTP_PASSWORD }}
      SMTP_FROM: ${{ secrets.SMTP_FROM }}
      SMTP_SECURITY: ${{ secrets.SMTP_SECURITY }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: "pip"

      - name: Install LibreOffice for PDF export
        run: |
          sudo apt-get update
          sudo apt-get install -y libreoffice-writer fonts-noto-cjk

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Compute report date
        id: report_date
        shell: bash
        run: |
          report_date="$(TZ=Asia/Shanghai date +%F)"
          echo "today=${report_date}" >> "$GITHUB_OUTPUT"
          echo "Report date: ${report_date}"

      - name: Restore email test marker
        id: email_marker
        if: github.event_name == 'schedule'
        uses: actions/cache/restore@v4
        with:
          path: .scheduled-email-test-marker
          key: scheduled-email-test-${{ steps.report_date.outputs.today }}

      - name: Skip duplicate email test
        if: github.event_name == 'schedule' && steps.email_marker.outputs.cache-hit == 'true'
        run: |
          echo "Scheduled email test already completed for ${{ steps.report_date.outputs.today }}; skipping."

      - name: Generate and email reports
        id: generate_report
        if: github.event_name != 'schedule' || steps.email_marker.outputs.cache-hit != 'true'
        shell: bash
        run: |
          mkdir -p ./output
          set +e
          overall=0
          report_date="${{ steps.report_date.outputs.today }}"
          for profile in chemistry biology statistics; do
            echo "::group::Generate and email ${profile} report"
            python main.py --profile "${profile}" --report-date "${report_date}" --output-dir ./output
            status=$?
            echo "::endgroup::"
            if [ "$status" -ne 0 ] && [ "$overall" -eq 0 ]; then
              overall="$status"
            fi
          done
          echo "exit_code=${overall}" >> "$GITHUB_OUTPUT"
          exit 0

      - name: Write email test marker
        if: github.event_name == 'schedule' && steps.email_marker.outputs.cache-hit != 'true' && steps.generate_report.outputs.exit_code == '0'
        run: |
          mkdir -p .scheduled-email-test-marker
          echo "${{ github.run_id }}" > ".scheduled-email-test-marker/${{ steps.report_date.outputs.today }}.txt"

      - name: Save email test marker
        if: github.event_name == 'schedule' && steps.email_marker.outputs.cache-hit != 'true' && steps.generate_report.outputs.exit_code == '0'
        uses: actions/cache/save@v4
        with:
          path: .scheduled-email-test-marker
          key: scheduled-email-test-${{ steps.report_date.outputs.today }}

      - name: Upload report artifacts
        if: always() && (github.event_name != 'schedule' || steps.email_marker.outputs.cache-hit != 'true')
        uses: actions/upload-artifact@v4
        with:
          name: scheduled-email-test-output
          path: |
            ./output/*.docx
            ./output/*.pdf
          if-no-files-found: error
          retention-days: 7

      - name: Fail workflow when report generation failed
        if: always() && (github.event_name != 'schedule' || steps.email_marker.outputs.cache-hit != 'true') && steps.generate_report.outputs.exit_code != '0'
        run: |
          code="${{ steps.generate_report.outputs.exit_code }}"
          if [ -z "$code" ]; then
            code=1
          fi
          echo "main.py exited with code ${code}"
          exit "$code"
